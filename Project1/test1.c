#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<windows.h>
#include<string.h>
#include<stdlib.h>



/*
* 
* int main() {
	char arr[] = "bit";
	char arr2[20] = "#######";
	strcpy(arr2, arr);
	printf("%c", arr2[3]);

	return 0;
}
* int m = 99, n = 5, r;
	while (m%n)
	{
		r = m % n;
		m = n;
		n = r;
	}
	printf("%d", 1%3);
* 
//nµÄ½×³Ë
	int i = 0, x = 0, sum = 1, sums = 1;
	scanf("%d", &i);
	for ( x = 1; x <= i; x++)
	{
		sum *= x;
		sums += sum;
	}
	printf("%d", sums);
	return 0;
*/
/*
char arr1[] = "welcome!!!!!!";
	char arr2[] = "#############";
	int left = 0;
	int len = sizeof(arr1) / sizeof(arr1[0]) - 2;
	int rigth = len;
	while (rigth >= left) {
		system("cls");
		arr2[left] = arr1[left++];
		arr2[rigth] = arr1[rigth--];
		printf("%s\n", arr2);
		Sleep(1000);

	}

	return 0;
*/